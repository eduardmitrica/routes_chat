# Copy to functions/.env (git-ignored). Loaded by the Firebase CLI at deploy.

# Named Firestore database the app reads and writes. This project has no
# "(default)" database. Must match FIRESTORE_DATABASE_ID in the app's .env.
FIRESTORE_DATABASE_ID=
